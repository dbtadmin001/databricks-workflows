apiVersion: v1
kind: ServiceAccount
metadata:
  name: spark-job-sa
  namespace: spark-jobs
  annotations:
    # IRSA annotation - replace with actual IAM role ARN after Terraform apply
    # Get from: terraform output -raw spark_job_role_arn
    eks.amazonaws.com/role-arn: "arn:aws:iam::<ACCOUNT_ID>:role/pos-retail-dev-spark-job-role"
